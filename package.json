{
  "name": "utility-cantiere",
  "version": "0.0.1",
  "private": true,
  "description": "Utility tecniche da cantiere - PWA e APK Android",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.0.0",
    "@capacitor/core": "8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.0.0"
  }
}
